import React from 'react';
import ReacDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', () => {
    const root = document.getElementById('root');
    ReacDOM.render(<h1>It is working</h1>, root);
});